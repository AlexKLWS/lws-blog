const home = '/'
const life = '/life'
const code = '/code'
const guides = '/guides'
const projects = '/projects'
const contact = '/contact'
const login = '/login'
const secret = {
  home: '/secret',
  editor: '/secret/editor',
  addPage: '/secret/add-page',
}

export default {
  home,
  life,
  code,
  guides,
  projects,
  contact,
  login,
  secret,
}
